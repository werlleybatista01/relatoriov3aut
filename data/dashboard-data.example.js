/**
 * Arquivo gerado automaticamente pelo atualizador Python.
 * Não edite manualmente em produção.
 *
 * A versão armazenada no pacote não contém dados reais.
 */
export const dashboardData = Object.freeze({
  schemaVersion: 2,
  metadata: Object.freeze({
    generatedAt: "",
    referenceDate: "",
    periodStart: "",
    periodEnd: "",
    operatingYear: 2026,
    seasonalHistoryYear: 2025,
    bagsPerPackage: 100,
    downloadUrl: ""
  }),
  features: Object.freeze({
    directWhatsAppEnabled: false,
    personalDataIncluded: false,
    homologationMode: true
  }),
  withdrawals: Object.freeze([]),
  stock: Object.freeze([]),
  openTools: Object.freeze([])
});
